stages:
- name: Composer Install
  steps:
  - runScriptConfig:
      image: anaxexp/wordpress-php:4-7.2-4.5.0
      shellScript: |-
        apt-get update
        apt-get install -y --no-install-recommends git zip libsqlite3-dev zlib1g-dev
        docker-php-ext-install zip
        curl --silent --show-error https://getcomposer.org/installer | php
        ./composer.phar install -n --prefer-dist
        cp .env.testing .env
- name: Publish image
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: wordpress-compose-example:${CICD_EXECUTION_SEQUENCE}
      registry: 127.0.0.1:34561
- name: Deploy
  steps:
  - applyYamlConfig:
      path: ./deploy/deployment.yaml
